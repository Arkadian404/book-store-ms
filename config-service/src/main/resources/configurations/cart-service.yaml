spring:
  datasource:
    url: jdbc:mysql://localhost:3307/cartdb?createDatabaseIfNotExist=true
    username: zafu
    password: zafu
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  client:
    book-url: http://localhost:8010/api/v1

server:
  port: 8030
